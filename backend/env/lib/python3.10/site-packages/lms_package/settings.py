from django.conf import settings

# undesn NOTIFICATION ий зам
NOTIFICATION_MODEL = settings.NOTIFICATION_MODEL if hasattr(settings, 'NOTIFICATION_MODEL') else "core.Notification"
NOTIFICATIONSTATE_MODEL = settings.NOTIFICATIONSTATE_MODEL if hasattr(settings, 'NOTIFICATIONSTATE_MODEL') else "lms.NotificationState"
NOTIFICATIONTYPE_MODEL = settings.NOTIFICATIONTYPE_MODEL if hasattr(settings, 'NOTIFICATIONTYPE_MODEL') else "core.NotificationType"

LOGIN_REQUIRED_PATH = settings.LOGIN_REQUIRED_PATH if hasattr(settings, 'LOGIN_REQUIRED_PATH') else "main.decorators.login_required"
