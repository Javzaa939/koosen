from django.apps import AppConfig
from django.core.checks import Tags, register

class LmsPackageAppConfig(AppConfig):
    name = "lms_package"
    verbose_name = "LMS iin back iin package"

    def ready(self) -> None:
        register(Tags.compatibility)
