from . import views
from django.urls import path

app_name = 'lms_package'
urlpatterns = [
    path('action/', views.NotificationAPIView.as_view()),
    path('state/<str:pk>/', views.NotifChangeStateApiView.as_view()),
    path('count/', views.NotifInfoApiView.as_view()),
]
