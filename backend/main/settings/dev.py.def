from .base import *

# NOTE: SECRET_KEY-ийг заавал оруулах!
SECRET_KEY = '123456'

# NOTE: production server дээр DEBUG заавал False байна!
DEBUG = True

# NOTE:Зөвшөөрөгдөх ip хаягууд болон domain-г оруулна уу!
ALLOWED_HOSTS = ['*']

# NOTE: Баазын тохиргооны хэсгийг заавал оруулна
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql_psycopg2',
        'NAME': '',
        'USER': '',
        'PASSWORD': '',
        'HOST':  '',
        'PORT': '',
    },
}

CORS_ALLOW_CREDENTIALS = True

CSRF_TRUSTED_ORIGINS = [
    'http://localhost:3000',
    'http://127.0.0.1:3000',
]

CORS_ORIGIN_WHITELIST = [
    'http://localhost:3000',
    'http://127.0.0.1:3000',
]
